# Publish Checklist

1. Verify `README.md` and `index.html` render correctly.
2. Ensure no secrets are present (`.env*`, tokens).
3. Push this folder contents to a clean GitHub repository.
4. Enable GitHub Pages from root branch if hosting `index.html`.
5. Keep model artifacts on Hugging Face and link from README.
